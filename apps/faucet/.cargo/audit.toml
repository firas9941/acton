[advisories]
ignore = [
  # SQLx records its optional MySQL driver's RSA dependency in Cargo.lock.
  # Faucet only enables SQLite; deny.toml forbids RSA in the resolved build graph.
  "RUSTSEC-2023-0071",
  "RUSTSEC-2024-0388", # derivative via ton_liteapi -> ton, also accepted in deny.toml
]

[output]
deny = ["yanked"]
quiet = false
